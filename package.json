{
  "name": "chipster",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "postinstall": "pnpm run build --filter=@micoblanc/chipster",
    "dev:docs": "pnpm run dev -w docs"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  },
  "packageManager": "pnpm@9.12.2"
}

